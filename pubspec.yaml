name: weather
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # External packages
  flutter_bloc: ^7.0.0
  get_it: ^7.1.3
  equatable: ^2.0.2

  # Personal packages
  data:
    path: ./packages/data
  domain:
    path: ./packages/domain


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true